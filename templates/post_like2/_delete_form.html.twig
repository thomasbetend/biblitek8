<form method="post" action="{{ path('app_post_like2_delete', {'id': post_like2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_like2.id) }}">
    <button class="btn">Delete</button>
</form>
