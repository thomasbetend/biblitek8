{% extends 'base.html.twig' %}

{% block title %}PostLike2{% endblock %}

{% block body %}
    <h1>PostLike2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_like2.id }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ post_like2.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_post_like2_index') }}">back to list</a>

    <a href="{{ path('app_post_like2_edit', {'id': post_like2.id}) }}">edit</a>

    {{ include('post_like2/_delete_form.html.twig') }}
{% endblock %}
