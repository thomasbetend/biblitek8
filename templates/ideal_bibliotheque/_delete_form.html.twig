<form method="post" action="{{ path('app_ideal_bibliotheque_delete', {'id': ideal_bibliotheque.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ideal_bibliotheque.id) }}">
    <button class="btn">Delete</button>
</form>
